## Leetcode solutions

The problem basically consist in traverse two for to have the efficiency that give the traditional arrays. First traverse the children array to find the largest value among these. Then it traverse and check if the max value is greater than the max value, if this is greater than or equal to the value, it save true, otherwise it save false. 